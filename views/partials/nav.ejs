<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">JOB PORTAL</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>

        <!-- Public Navigation -->
        <li class="nav-item">
          <a class="nav-link" href="/jobs">Browse Jobs</a>
        </li>

        <!-- Student-Only Navigation -->
        <% if (locals.user && locals.user.role === 'student') { %>
          <li class="nav-item">
            <a class="nav-link" href="/student/applications">My Applications</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/student/profile">Profile</a>
          </li>
        <% } %>

        <!-- Recruiter-Only Navigation -->
        <% if (locals.user && locals.user.role === 'recruiter') { %>
          <li class="nav-item">
            <a class="nav-link" href="/recruiter/post-job">Post Job</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/recruiter/my-jobs">My Jobs</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/recruiter/applicants">Applicants</a>
          </li>
        <% } %>

        <!-- Admin-Only Navigation -->
        <% if (locals.user && locals.user.role === 'admin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin/users">Manage Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/jobs">Manage Jobs</a>
          </li>
        <% } %>

        <!-- User Dropdown / Auth -->
        <% if (locals.user) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= locals.user.email %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/user/profile">Profile Settings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/user/signup">Signup</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user/signin">Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="container mt-4">
    <div class="alert alert-danger" role="alert">
      <%= locals.error %>
    </div>
  </div>
<% } %>